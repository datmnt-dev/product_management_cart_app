import 'package:flutter/material.dart';

/// Root navigator for immersive routes (product detail/form) above the shell.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>(
  debugLabel: 'root',
);
